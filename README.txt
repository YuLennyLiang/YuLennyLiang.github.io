README

1. Edit config.toml accordingly with any plain text editor (notepad, notepad++, sublime text, etc)
2. Run "run.bat" to use hugo.exe to build site and output the files to the "public" directory.

